<%= error_messages_for 'query' %>
<% if @query_bookings.valid? %>
    <% if @bookings.empty? %>
        <h3><%= l(:time_tracker_label_your_time_bookings) %></h3>
        <p class="nodata"><%= l(:label_no_data) %></p>
    <% else %>
        <%= render :partial => 'time_bookings/list_bookings', :locals => {:bookings => @bookings, :query => @query_bookings} %>
        <p class="pagination"><%= pagination_links_full @booking_pages, @booking_count, :page_param => :page_bookings, :per_page_links => false %></p>
    <% end %>
<% end %>