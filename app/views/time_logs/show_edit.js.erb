<% @time_logs.each do |item| %>

$('#<%= "entry-" + item.id.to_s %>').html('<%= escape_javascript(render(:partial => 'time_logs/edit_form', :locals => {:time_log => item})) %>');
$('#<%= "entry-"+ item.id.to_s %>').removeClass('hascontextmenu');
<% end %>
contextMenuHide();
contextMenuUnselectAll();
redmine_time_tracker.TimeTracker.hideMultiFormButtons('tl_edit_form_update_button');